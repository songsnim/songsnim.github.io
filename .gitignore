# build tooling — kept out of the Obsidian vault's sync and search
site/node_modules/
site/dist/
site/.astro/

# macOS / editor cruft
.DS_Store
# Obsidian's Space plugin leaves one of these in every folder it has opened.
.space/
*.sync-conflict-*
